services:
  - type: web
    name: hydrotech-ai-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter # Free plan usually has 512MB RAM which might be tight for PyTorch, so 'starter' or 'standard' is recommended for ML models.
    region: ohio
    envVars:
      - key: PORT
        value: 8000
